# Minimal requirements for CI (pytest + docker build).
# Avoids conflitos do pip freeze local (streamlit exige numpy<2 e pandas<3).
# O requirements.txt completo é usado apenas no ambiente local de desenvolvimento.

# PyTorch CPU (instalado separadamente via --index-url no workflow)
# torch==2.11.0
# torchvision==0.26.0

# Core ML
numpy>=1.26,<2
pandas>=1.4,<3
scikit-learn>=1.3
scipy>=1.11
Pillow>=9.0
opencv-python-headless>=4.10,<5
timm>=1.0
albumentations>=1.3
PyYAML>=6.0
requests>=2.28
tqdm>=4.0
matplotlib>=3.7

# Env
python-dotenv>=1.0

# MLflow
mlflow>=2.7

# API
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# AWS
boto3>=1.34
botocore>=1.34

# Logging
python-json-logger>=2.0

# Testing
pytest>=8.0
httpx>=0.27

# Feature Store (feast instalado com --no-deps para evitar conflito numpy<2)
pyarrow>=14.0
pytz
