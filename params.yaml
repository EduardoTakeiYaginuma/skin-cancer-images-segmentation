train:
  model_arch: resnet50          # efficientnet_b0 | resnet50
  input_size: 224
  batch_size: 32
  lr: 0.0001
  epochs: 20
  augmentation: true            # true | false
  neg_ratio: 3.0                # non-melanoma images per melanoma image
  num_workers: 4
  seed: 42
